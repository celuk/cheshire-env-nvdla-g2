diff --git a/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
index 34226ea..23da979 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
@@ -574,6 +574,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [10:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 11;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +670,8 @@ output [10:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
index 4998aff..66832fa 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
@@ -574,6 +574,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [5:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 6;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +670,8 @@ output [5:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
index c7c7631..8c422b6 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
@@ -424,6 +424,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [127:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 128;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +520,8 @@ output [127:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
index b404142..71adeae 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
@@ -424,6 +424,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 14;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +520,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
index 650bac2..2c71cac 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
@@ -424,6 +424,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +520,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
index 1c8f413..45a403a 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
@@ -424,6 +424,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [255:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +520,8 @@ output [255:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
index 08605dd..7d4640d 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
@@ -429,6 +429,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [271:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 272;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +525,8 @@ output [271:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
index 5548288..e3c5ce3 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
@@ -429,6 +429,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +525,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
index c700898..475bd6c 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
@@ -424,6 +424,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +520,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
index de73dcc..ea4e9a4 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
@@ -436,6 +436,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +532,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
index f7d8b75..af5d3c3 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
@@ -441,6 +441,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [287:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +537,8 @@ output [287:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
index 71bc39f..732c908 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
@@ -436,6 +436,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 32;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +532,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
index ddbcbaf..a51cd23 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
@@ -441,6 +441,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [79:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 80;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +537,8 @@ output [79:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
index c392012..d66fe3e 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
@@ -436,6 +436,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 8;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +532,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
index bc8af2e..7f7b3b2 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [3:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 4;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [3:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
index 0beca22..3c109d1 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [6:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 7;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [6:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
index cd77293..e4ad725 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 8;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
index 42a3813..545f99b 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
@@ -445,6 +445,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +541,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
index 1969fbc..27dad30 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
@@ -445,6 +445,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 32;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +541,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
index 0b749ec..315e3b4 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
@@ -440,6 +440,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -519,6 +536,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
index 2d611ef..0d1db6a 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
@@ -476,6 +476,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [21:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 22;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -555,6 +572,8 @@ output [21:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
index b54f48b..6997f3b 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
@@ -510,6 +510,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [9:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 10;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +606,8 @@ output [9:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
index 17cdbb0..05d2055 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
@@ -505,6 +505,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 14;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -584,6 +601,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
index 4073c77..1904676 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
@@ -505,6 +505,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -584,6 +601,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
index e85c431..99f97c2 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
@@ -510,6 +510,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 18;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +606,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
index c1b4ec2..c69cfe9 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
@@ -472,6 +472,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 32;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -551,6 +568,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
index 3557d03..1bb09a6 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
@@ -529,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 14;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -608,6 +625,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
index 6cb9768..f082e87 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
@@ -534,6 +534,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [14:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 15;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +630,8 @@ output [14:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
index 8458e8e..d290ea4 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
@@ -496,6 +496,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 18;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -575,6 +592,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
index 375f1d5..6f4233f 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
@@ -529,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [8:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 9;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -608,6 +625,8 @@ output [8:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
index b1b284a..b436ec5 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
@@ -416,6 +416,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [129:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 130;
+    localparam dwords = 8;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +512,8 @@ output [129:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
index f21bedf..eef6cc5 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
@@ -416,6 +416,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [257:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 258;
+    localparam dwords = 8;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +512,8 @@ output [257:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
index 58cbe53..9b774a7 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
@@ -416,6 +416,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 8;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +512,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
index 5b29f5a..d4c2e6b 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
@@ -789,6 +789,23 @@ input [6:0] RA, WA;
 output [127:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 128;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -970,6 +987,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
index 3cdd88c..802bda7 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
@@ -835,6 +835,23 @@ input [7:0] RA, WA;
 output [17:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 18;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1036,6 +1053,8 @@ reg [17:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
index 83730e7..36385a7 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
@@ -789,6 +789,23 @@ input [6:0] RA, WA;
 output [223:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 224;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -970,6 +987,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
index 162e3b8..3adcb00 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
@@ -795,6 +795,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -976,6 +993,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
index 1a6e95f..00ed516 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
@@ -789,6 +789,23 @@ input [6:0] RA, WA;
 output [257:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 258;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -970,6 +987,8 @@ reg [257:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
index 47eac09..5dc482e 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
@@ -789,6 +789,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -970,6 +987,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
index f8b0c81..ddc02a6 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
@@ -829,6 +829,23 @@ input [7:0] RA, WA;
 output [31:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 32;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1030,6 +1047,8 @@ reg [31:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
index 3c72b16..ee2b5e3 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
@@ -789,6 +789,23 @@ input [6:0] RA, WA;
 output [59:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 60;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -970,6 +987,8 @@ reg [59:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
index a0a6e43..cdf48be 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
@@ -795,6 +795,23 @@ input [6:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -976,6 +993,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
index 6a1e771..78e05bd 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
@@ -861,6 +861,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1062,6 +1079,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
index 7f9d38b..56c33fa 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
@@ -861,6 +861,23 @@ input [7:0] RA, WA;
 output [15:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1062,6 +1079,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
index ac66240..50ff0ee 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
@@ -867,6 +867,23 @@ input [7:0] RA, WA;
 output [64:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 65;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1068,6 +1085,8 @@ reg [64:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
index 473cb85..272bd49 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
@@ -861,6 +861,23 @@ input [7:0] RA, WA;
 output [81:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 82;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1062,6 +1079,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
index f4d8aff..58654e9 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
@@ -861,6 +861,23 @@ input [7:0] RA, WA;
 output [92:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 93;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1062,6 +1079,8 @@ reg [92:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
index 12b2897..eea5a80 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
@@ -955,6 +955,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 248;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1155,6 +1172,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
index dea1a7c..780379d 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
@@ -955,6 +955,23 @@ input [7:0] RA, WA;
 output [81:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 82;
+    localparam dwords = 248;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1155,6 +1172,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
index 786dccd..f8bd3df 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
@@ -957,6 +957,23 @@ input [7:0] RA, WA;
 output [111:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 112;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1156,6 +1173,8 @@ reg [111:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
index a9c7860..e68af74 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
@@ -957,6 +957,23 @@ input [7:0] RA, WA;
 output [112:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 113;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1156,6 +1173,8 @@ reg [112:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
index b8e28c9..1998c31 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
@@ -1025,6 +1025,23 @@ input [8:0] RA, WA;
 output [10:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 11;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1258,6 +1275,8 @@ reg [10:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
index a68ef8f..6c3facc 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
@@ -957,6 +957,23 @@ input [7:0] RA, WA;
 output [127:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 128;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1156,6 +1173,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
index 0eb14b4..29a4b08 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
@@ -957,6 +957,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1156,6 +1173,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
index 49c58d2..d67ad5c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
@@ -1019,6 +1019,23 @@ input [8:0] RA, WA;
 output [13:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 14;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1252,6 +1269,8 @@ reg [13:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
index 4fc1554..6eca357 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
@@ -957,6 +957,23 @@ input [7:0] RA, WA;
 output [15:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1156,6 +1173,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
index aa028e3..390646b 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
@@ -963,6 +963,23 @@ input [7:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1162,6 +1179,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
index fd5f968..92700f6 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
@@ -963,6 +963,23 @@ input [7:0] RA, WA;
 output [79:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 80;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1162,6 +1179,8 @@ reg [79:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
index 4a75907..397fcfe 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
@@ -693,6 +693,23 @@ input [6:0] RA, WA;
 output [127:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 128;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -876,6 +893,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
index 7cc4359..309eba3 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
@@ -693,6 +693,23 @@ input [6:0] RA, WA;
 output [129:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 130;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -876,6 +893,8 @@ reg [129:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
index 76bc1af..b6ecff2 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [191:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 192;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [191:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
index 8dc6415..02a56b6 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [223:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 224;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
index 6c8c109..ce12dd9 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
@@ -693,6 +693,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -876,6 +893,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
index ee4ea9c..f40e4cb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [271:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 272;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [271:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
index 8b8180a..6e0b7b7 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
index 708bfdb..956006e 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [39:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 40;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [39:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
index d80a1e5..e237880 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
@@ -1275,6 +1275,23 @@ input [8:0] RA, WA;
 output [55:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 56;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1506,6 +1523,8 @@ reg [55:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
index 49195cc..3f0902b 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
index 37c360d..f22b69c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [71:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 72;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
index f30712a..bc72c52 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [7:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 8;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [7:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
index 20e5d3d..5f675c6 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
@@ -721,6 +721,23 @@ input [6:0] RA, WA;
 output [167:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 168;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -905,6 +922,8 @@ reg [167:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
index ad1ace0..29cb8ad 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
@@ -721,6 +721,23 @@ input [6:0] RA, WA;
 output [41:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 42;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -905,6 +922,8 @@ reg [41:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
index be09abc..13e1a30 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
@@ -721,6 +721,23 @@ input [6:0] RA, WA;
 output [83:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 84;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -905,6 +922,8 @@ reg [83:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
index c06fdf8..83d35b4 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [115:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 116;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [115:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
index bf161e4..cc2833f 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
@@ -725,6 +725,23 @@ input [6:0] RA, WA;
 output [127:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 128;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -908,6 +925,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
index bb21a6f..0319529 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
@@ -725,6 +725,23 @@ input [6:0] RA, WA;
 output [129:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 130;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -908,6 +925,8 @@ reg [129:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
index 8aae9be..acd9656 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [159:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 160;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [159:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
index cf976c1..835a27c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [223:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 224;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
index 80cec6d..bd83325 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [225:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 226;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
index 983fec6..1cca89c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
@@ -725,6 +725,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -908,6 +925,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
index 1aa844d..0d150bb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
index 905a763..80d1f5e 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
index 8f87fb5..81468cb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [65:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
index ccd0ab8..0e7544a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
@@ -781,6 +781,23 @@ input [7:0] RA, WA;
 output [15:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -984,6 +1001,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
index cc67ddc..9677c50 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
@@ -781,6 +781,23 @@ input [7:0] RA, WA;
 output [16:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 17;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -984,6 +1001,8 @@ reg [16:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
index 23fb54e..b79a7dc 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
@@ -741,6 +741,23 @@ input [6:0] RA, WA;
 output [225:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 226;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -924,6 +941,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
index b6fe412..0dfd791 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
@@ -741,6 +741,23 @@ input [6:0] RA, WA;
 output [237:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 238;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -924,6 +941,8 @@ reg [237:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
index f72ea46..0850bab 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
index fb90f47..a671aa9 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
@@ -741,6 +741,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -924,6 +941,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
index 2ce5420..1c8f816 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
@@ -741,6 +741,23 @@ input [6:0] RA, WA;
 output [35:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 36;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -924,6 +941,8 @@ reg [35:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
index e11f7be..6d3fe6a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [65:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
index 1b22c61..bfb5ecd 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
@@ -741,6 +741,23 @@ input [6:0] RA, WA;
 output [71:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 72;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -924,6 +941,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/nvdla_small.v b/vsrc/small/nvdla_small.v
index 69c4904..da5c189 100644
--- a/vsrc/small/nvdla_small.v
+++ b/vsrc/small/nvdla_small.v
@@ -84,7 +84,7 @@ NV_NVDLA_apb2csb apb2csb (
 NV_nvdla nvdla_top (
    .dla_core_clk                    (core_clk)
   ,.dla_csb_clk                     (csb_clk)
-  ,.global_clk_ovr_on               (1'b0)
+  ,.global_clk_ovr_on               (1'b1)
   ,.tmc2slcg_disable_clock_gating   (1'b0)
   ,.dla_reset_rstn                  (rstn)
   ,.direct_reset_                   (1'b1)
diff --git a/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
index 34226ea..23da979 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
@@ -574,6 +574,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [10:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 11;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +670,8 @@ output [10:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
index 4998aff..66832fa 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
@@ -574,6 +574,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [5:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 6;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +670,8 @@ output [5:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
index 999f860..e9182d9 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
@@ -429,6 +429,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [255:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +525,8 @@ output [255:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
index 08605dd..7d4640d 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
@@ -429,6 +429,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [271:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 272;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +525,8 @@ output [271:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
index 5548288..e3c5ce3 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
@@ -429,6 +429,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 16;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +525,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
index f7d8b75..af5d3c3 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
@@ -441,6 +441,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [287:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +537,8 @@ output [287:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
index ddbcbaf..a51cd23 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
@@ -441,6 +441,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [79:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 80;
+    localparam dwords = 20;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +537,8 @@ output [79:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
index bc8af2e..7f7b3b2 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [3:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 4;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [3:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
index 0beca22..3c109d1 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [6:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 7;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [6:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
index cd77293..e4ad725 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
@@ -702,6 +702,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 8;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +798,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
index 42a3813..545f99b 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
@@ -445,6 +445,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +541,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
index 1969fbc..27dad30 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
@@ -445,6 +445,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 32;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +541,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
index d85ccbb..de09024 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
@@ -481,6 +481,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [21:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 22;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -560,6 +577,8 @@ output [21:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
index b54f48b..6997f3b 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
@@ -510,6 +510,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [9:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 10;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +606,8 @@ output [9:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
index e85c431..99f97c2 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
@@ -510,6 +510,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 18;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +606,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
index b236466..ef7e9c4 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
@@ -534,6 +534,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 14;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +630,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
index 6cb9768..f082e87 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
@@ -534,6 +534,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [14:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 15;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +630,8 @@ output [14:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
index 7ba9e4b..bdf1ca2 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
@@ -534,6 +534,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [8:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 9;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +630,8 @@ output [8:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
index 58cbe53..9b774a7 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
@@ -416,6 +416,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 8;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +512,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
index 3cdd88c..802bda7 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
@@ -835,6 +835,23 @@ input [7:0] RA, WA;
 output [17:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 18;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1036,6 +1053,8 @@ reg [17:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
index 162e3b8..3adcb00 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
@@ -795,6 +795,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -976,6 +993,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
index a0a6e43..cdf48be 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
@@ -795,6 +795,23 @@ input [6:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 128;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -976,6 +993,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
index f691b6b..fbeb610 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
@@ -867,6 +867,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1068,6 +1085,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
index 3ba4c18..d23fa5c 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
@@ -867,6 +867,23 @@ input [7:0] RA, WA;
 output [15:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1068,6 +1085,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
index ac66240..50ff0ee 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
@@ -867,6 +867,23 @@ input [7:0] RA, WA;
 output [64:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 65;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1068,6 +1085,8 @@ reg [64:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
index 9bafc68..a4e04d1 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
@@ -867,6 +867,23 @@ input [7:0] RA, WA;
 output [81:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 82;
+    localparam dwords = 160;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1068,6 +1085,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
index 12b2897..eea5a80 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
@@ -955,6 +955,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 248;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1155,6 +1172,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
index dea1a7c..780379d 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
@@ -955,6 +955,23 @@ input [7:0] RA, WA;
 output [81:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 82;
+    localparam dwords = 248;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1155,6 +1172,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
index b8e28c9..1998c31 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
@@ -1025,6 +1025,23 @@ input [8:0] RA, WA;
 output [10:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 11;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1258,6 +1275,8 @@ reg [10:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
index 050d585..f9ed75f 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
@@ -963,6 +963,23 @@ input [7:0] RA, WA;
 output [143:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 144;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1162,6 +1179,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
index aa028e3..390646b 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
@@ -963,6 +963,23 @@ input [7:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1162,6 +1179,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
index fd5f968..92700f6 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
@@ -963,6 +963,23 @@ input [7:0] RA, WA;
 output [79:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 80;
+    localparam dwords = 256;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1162,6 +1179,8 @@ reg [79:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
index 76bc1af..b6ecff2 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [191:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 192;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [191:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
index 8dc6415..02a56b6 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [223:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 224;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
index e4140d1..feed0fb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
index ee4ea9c..f40e4cb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [271:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 272;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [271:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
index 8b8180a..6e0b7b7 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
@@ -699,6 +699,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 32;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -882,6 +899,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
index 708bfdb..956006e 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [39:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 40;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [39:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
index 49195cc..3f0902b 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
index 37c360d..f22b69c 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [71:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 72;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
index f30712a..bc72c52 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
@@ -1281,6 +1281,23 @@ input [8:0] RA, WA;
 output [7:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 8;
+    localparam dwords = 512;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -1512,6 +1529,8 @@ reg [7:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
index bcac485..b0fcab8 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
@@ -727,6 +727,23 @@ input [6:0] RA, WA;
 output [167:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 168;
+    localparam dwords = 60;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -911,6 +928,8 @@ reg [167:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
index c06fdf8..83d35b4 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [115:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 116;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [115:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
index 8aae9be..acd9656 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [159:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 160;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [159:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
index cf976c1..835a27c 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [223:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 224;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
index 80cec6d..bd83325 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [225:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 226;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
index 1aa844d..0d150bb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
index 905a763..80d1f5e 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [63:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 64;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
index 8f87fb5..81468cb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
@@ -731,6 +731,23 @@ input [6:0] RA, WA;
 output [65:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 64;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -914,6 +931,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
index dbd5e85..100cff8 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
@@ -787,6 +787,23 @@ input [7:0] RA, WA;
 output [15:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 16;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -990,6 +1007,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
index ed8a3f9..27343f0 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [225:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 226;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
index f72ea46..0850bab 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [255:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 256;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
index b3d3336..f464f05 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [287:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 288;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
index e11f7be..6d3fe6a 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [65:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 66;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
index b9a5672..1f32632 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
@@ -747,6 +747,23 @@ input [6:0] RA, WA;
 output [71:0] RD;
 input [7:0] SLEEP_EN;
 input RET_EN , clobber_array , clobber_flops ;
+`ifdef SYNTHESIS
+    // Force BRAM inference for FPGA
+    localparam dbits = 72;
+    localparam dwords = 80;
+    (* ram_style = "block" *) reg [dbits-1:0] mem [0:dwords-1];
+    reg [dbits-1:0] r_data;
+
+    always @(posedge CLK) begin
+        if (WE) mem[WA] <= WD;
+    end
+
+    always @(posedge CLK) begin
+        if (RE) r_data <= mem[RA];
+    end
+    assign RD = r_data;
+`else
+
 // When there is no bypass requested, tie the internal bypass select to 0.
     wire RDBYP = 1'b0;
 //Definitions of latches and flops in the design
@@ -930,6 +947,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
